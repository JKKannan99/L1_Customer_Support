# import bleach

# def sanitize_text(text: str) -> str:
#     return bleach.clean(
#         text,
#         tags=[],          # ❌ remove all HTML tags
#         attributes={},
#         strip=True
#     )
